[project]
name = "reverse-text-proof-v1"
version = "0.1.0"
description = "A deterministic short-text reversal task for distributed RL validation."
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["reverse_text_proof_v1"]
