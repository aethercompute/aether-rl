[project]
name = "codeword-v1"
version = "0.1.0"
description = "A tiny hidden codeword classification task for distributed RL validation."
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["codeword_v1"]
