[project]
name = "countdown-proof-v1"
version = "0.1.0"
description = "Synthetic countdown arithmetic tasks for distributed RL validation."
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["countdown_proof_v1"]
