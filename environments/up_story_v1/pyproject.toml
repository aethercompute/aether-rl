[project]
name = "up-story-v1"
version = "0.1.0"
description = "A fixed-prompt substring-repetition task for distributed RL validation."
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["up_story_v1"]
