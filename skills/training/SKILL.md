---
name: training
description: Launch and monitor distributed Aether RL training. Routes to start-run and monitor-run.
---

# Training

- Use `start-run/SKILL.md` to preflight and launch the coordinator and outbound workers.
- Use `monitor-run/SKILL.md` to inspect health, leases, result queues, policies, and trainer progress.
- Use the `configs` skill for TOML and CLI override mechanics.
- Use the `install` skill for role and environment packages.
- Use `docs/troubleshooting.md` for identity, connectivity, vLLM, spool, and restart failures.
