# Aether RL Configs

Shared Pydantic configuration models for Aether RL coordinator, worker, trainer, and worker-local inference processes.

```bash
pip install aether-rl-configs
```

Primary models live in `aether_rl.configs.server`, `worker`, `trainer`, and `inference`.
